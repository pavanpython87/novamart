# Warehouse backend: duckdb (default, no cloud needed) or bigquery
WAREHOUSE_BACKEND=duckdb

# Google BigQuery (optional — only needed for WAREHOUSE_BACKEND=bigquery)
BQ_PROJECT_ID=
BQ_DATASET=novamart
GOOGLE_APPLICATION_CREDENTIALS=./gcp-key.json

# Pipeline
LOG_LEVEL=INFO
CHAOS_LEVEL=default
